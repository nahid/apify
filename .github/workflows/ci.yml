name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-and-test:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        dotnet-version: ['8.0.x']

    runs-on: ${{ matrix.os }}
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: ${{ matrix.dotnet-version }}
    
    - name: Restore dependencies
      run: dotnet restore
    
    - name: Build
      run: dotnet build --no-restore
    
    - name: Run Init Command
      run: dotnet run init --name "Test API Project" --base-url "https://api.example.com" --force
      
    - name: Run GET Tests
      run: dotnet run run apis/sample-api.json

    - name: Run POST Tests
      run: dotnet run run apis/sample-post.json
      
    - name: List environments
      run: dotnet run list-env